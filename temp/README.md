# temp/

Scratch output from Cursor skills. Layout:

```
temp/<skill-name>/<file>.md|.ls|.kl|.txt
```

Do not copy these into `docs/` or `programs/` until the user accepts the draft. Contents of this folder (except this README and `.gitkeep`) are gitignored.
